//This archive only in directive to user.

function automatic()
const read =  require('read');
const fs = require('fs');
const readInterface = automatic.createInterface({
  input: fs.createReadStream('archive.txt'),
  output: process.stdout,
  console: false
  });
  
  readInterface.on('line', function(line) {
    console.log(line);
    
    });
    };
let Connected = require('ssh');
let load = new Connected();
load.on('connect', function() {
  console.log('Connection :: connect');
});
lpad.on('error', function(err) {
  console.log('Connection :: error :: ' + err);
});
c.connect({
  host: 'ip',
  port: 2801,
  username: 'name',
  password: require(fs)
});
    //Archive this read a file to use a create new server.
const express = require('express');
const server = express();
const ('name') = require("archive.json");

server.use(express.json());

server.get("/", function(req, res){
    res.txt(('name'));
});

app.listen(3000, function(){
    console.log("OK - Collecting informations to server.");
  
  
